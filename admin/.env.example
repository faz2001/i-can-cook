# Base URL of the same running FastAPI backend used by the consumer app
# (backend_final_pkg). The admin panel is a separate frontend but talks to
# the exact same API -- no backend changes are needed to run it.
VITE_API_BASE_URL=http://localhost:8000
